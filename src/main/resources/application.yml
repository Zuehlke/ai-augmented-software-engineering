spring:
  application:
    name: flight-api
  datasource:
    url: jdbc:h2:mem:flightsdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
  sql:
    init:
      mode: always

server:
  port: 8080

springdoc:
  show-actuator: true