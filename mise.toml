[tools]
java = "25"
maven = "3"
node = "24"

[tasks.switch]
description = "Stop app, stash changes and switch branch"
usage = 'arg "<branch>" help="Target branch"'
run = """
kill $(lsof -ti tcp:8080) 2>/dev/null || true
git stash push -u -m auto-stash || true
git switch "$usage_branch"
"""